class_name Player
extends Node2D
## Player: the thing the camera follows and the streaming distances measure from.
##
## Empty placeholder (M0). The Ship (RigidBody2D with engine components) becomes
## its child in M1.


func _ready() -> void:
	pass
